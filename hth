while true; do
    echo -n "Enter a guess: "
    read guess

    if ! [[ "$guess" =~ ^[0-9]+$ ]]; then
        echo "Erorr: enter an integer!"
        continue
    fi
    if [ "$guess" -lt 1 ] || [ "$guess" -gt 100 ]; then
        echo "1 to 100"
        continue
    fi
